# Security review — 2026-09-21 (second of the day)

Mode: change (the Fractured Fist table after its design: one supply shelf,
the strike gutter, step-scoped counters, the action bar, the strike moment,
"play all resources", and the loadout as a grouped pick)
Commit and working-tree scope: the working tree on top of commit 50a9abd on
`main`, committed in pieces right after this report. Changed surfaces: the
web app only (`apps/web/src/{glue/fractured-fist.ts,glue/types.ts,
glue/agency.ts,glue/ZoneRenderer.tsx,pages/Table.tsx,pages/Setup.tsx,
pages/GroupedPick.tsx,table/parts.tsx,table/StrikeMoment.tsx,
playback/PlaybackQueue.ts,playback/usePlaybackQueue.ts,app.css}`), the
primitives package (`CardData.counts` and its rendering), the tests, the
browser specs, three design artboards and one SVG under `docs/design`, and
the docs. No server, wire-contract, database or dependency change.
Environment: local machine, synthetic fixtures only; unit tests against the
scripted fake socket and fixture views; the browser specs against the local
stack (sibling engine, server on a local SQLite file, Vite).
Prior full review: none; the prior change review is
[2026-09-21-64bfd4d-change.md](2026-09-21-64bfd4d-change.md).
Overall result: no new issues in the tested scope; one pre-existing
disclosure noted below; the earlier findings still open.
Release recommendation: not applicable (no deployment yet).

## Coverage

Selected under change mode: SEC-01, SEC-02, SEC-14 and SEC-18 (always) and
the "tables, sockets, moves, events, replay, game glue" row (SEC-02,
SEC-05, SEC-07 through SEC-10, SEC-13, SEC-16, SEC-17). The server did not
change, so SEC-05, SEC-13, SEC-16 and SEC-17 were inspected for the client
side only. SEC-03, SEC-04, SEC-06, SEC-11, SEC-12, SEC-15, SEC-19 through
SEC-22 are outside this change's scope and were not run; their code did
not change.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No new route, socket event or outbound request. The browser still sends moves only through the existing `move` socket message; the new action bar, the batch and the moment add no transport | |
| SEC-02 | Authorization | PASS | Unchanged on the server. On the page, the shelf's stacks are keyed by the viewer's own seat and only that seat's `buy_card` moves light a stack; the opponent's counts are drawn but never selectable (`glue.test.ts`: "one shelf with both players' counts") | |
| SEC-07 | Hidden information | PASS | Everything the shelf, the gutter, the counters and the strike moment read is in the engine's public part of the view (`supply`, `damage_queued`, `defense_queued`, `stamina`, `max_stamina`, `misstep_count`, the per-turn counters): the engine's `commonPublic` publishes them to both seats and to watchers. The moment reads the two views the seat already received; it asks for nothing new. A watcher's plan lists both players in the side column and no hand (`glue.test.ts` runs the shelf with no seat) | |
| SEC-09 | Game actions and agency | PASS (reproduced) | The action bar offers a button only for a move the engine listed at that moment (advance, end turn, focus reload, skip refine) and sends it as a tap through `submitMove`; a button for an unlisted move is not drawn (`table.test.tsx`: "an action-bar button submits the listed move"; `glue.test.ts`: "the action bar holds the moves that move the turn"). The batch is one press by the person: the table finds the move for each next card only among the legal moves the engine returned after the previous one landed, sends it through the policy's new `batch` trigger with the same listed-move test as a tap, and stops when a card means nothing or several things (`table.test.tsx`: "play all resources"; `agency.ts`). The strike moment submits nothing and asks nothing; it holds the event and lets it land when its beats are done or skipped (`table.test.tsx`: "the strike plays as a moment"; `queue.test.ts`: "a gate holds the next event"). No draw is faked: the engine resolves every draw inside the move that causes it and the deck never lights up (plan section 13). The loadout preselects nothing; the default seven is a button (`GroupedPick.tsx`; `e2e/helpers.ts` asserts no card is picked before the first press). A full game against the live engine, every move a tap or a menu press, ends on the end panel (`e2e/fractured-fist.spec.ts`) | The `Replay the strike` button re-runs playback's `replayLast`, which is read-only |
| SEC-09 (second copies) | No rule copied into Universe | PASS | The gutter shows the queued numbers without subtracting; the moment's "through" is the stamina difference between the engine's two views, and the "absorbed" caption is the difference between two engine numbers, used for nothing (`glue.test.ts`: "reads the strike from the views before and after"). Card names, costs, values, effects, schools, the default seven and the misstep cap come from the reference data; the reload cap is not shown because the engine does not publish it | |
| SEC-10 | Validation and injection | PASS | Every string from the engine (names, descriptions, summaries, seat names) is rendered as React text; no `innerHTML`, no `dangerouslySetInnerHTML`, no URL built from game data (searched the changed files). The setup answers stay typed: exactly seven values from the option list, sent as before | |
| SEC-14 | Secrets and published artifacts | PASS with a note | Changed and new files scanned for local paths, addresses and token-like strings: none. The three artboards and `punch-blast.svg` are Zekel Games' own design work; the sample names in the artboards (a first name, "Rook") are drawing placeholders. Note: the tracked game references `docs/games/*.md` list local machine paths under "Sources" from an earlier session; recorded as F09 below | |
| SEC-16 | Abuse and cost (client) | PASS | The batch sends at most one move per event it receives and never re-sends for the same event (`batchSentFor`); the gate holds the queue rather than looping; the moment's timers are cleared on unmount. No new polling | |
| SEC-17 | Logs | PASS | No new logging; the agency policy's existing refusal warning is unchanged | |
| SEC-18 | Dependencies, CI | PASS with note | No dependency change; `pnpm typecheck`, `pnpm --filter @universe/web build`, the web and server unit tests and two browser specs pass | F01 unchanged |

## Findings

- **SEC-14-F09 (low, disclosure).** The four game references under
  `docs/games/` end with a "Sources" list of absolute paths on the
  author's machine, against the repository's own rule in `CLAUDE.md`
  ("no local machine paths"). Not part of this change; it predates it.
  Fix: replace each path with the repository and file it names (the
  engine's `src/games/<game>/...`, the port folder's document names).

### Earlier findings

- SEC-18-F01, SEC-08-F02, SEC-04-F03, SEC-15-F07, SEC-16-F08: open, unchanged.

## Execution

Commands/tools and versions: Node 24 locally (CI uses 22), pnpm 10, vitest
2, Playwright 1.63 with Chromium, the sibling engine at its current commit.
Tests passed/failed/skipped; real engine versus stub: web 6 files, 63 tests
passed (new: the shelf, the gutter, the scoped counters, the bought card's
flight, the action bar, the batch, the strike moment, the grouped loadout,
the queue's gate); server 9 files passed, 1 skipped (unchanged); typecheck
and the production web build clean. Browser specs against the live engine:
`fractured-fist.spec.ts` (a full game, 2.4 minutes) and
`storefront.spec.ts` passed; `sign-in.spec.ts` and `by-turns.spec.ts` were
edited for the new loadout picker but not run (they need the review server
with the test outbox).
Browser/transport/configuration coverage: Chromium through Playwright;
development configuration.
Dependency and secret scan scope/date: no dependency change; changed and
new files scanned on 2026-09-21.
Checks blocked/not run and exact reason: the two outbox specs above;
Postgres and compose (Docker unavailable); the domains listed above as
outside scope.
Prior findings rechecked, fixed, reopened, or still unverified: all
rechecked against the diff, still open, surfaces unchanged; F09 added.
Files changed and temporary-resource cleanup: no temporary resources; the
Playwright results folder is ignored by git.
Next required review: a full review at the first development session of the
next active week (still due), and a release review before the first deploy.

## Entry-point inventory at this revision

Unchanged: no REST, socket, static or outbound addition.
