# Security review — 2026-09-22

Mode: change (the first two items of the Cybernoir table's design pass:
printed names instead of ids, and the clue rail both seats share)
Commit and working-tree scope: the working tree on top of commit ca3d616 on
`main`, committed right after this report. Changed surfaces: one glue module
(`apps/web/src/glue/cybernoir-2127.ts`), its two test files, two lines of the
shared primitives (the Pool's count and the track space's shape), and the
implementation plan. No server, wire, database, dependency or configuration
change, and no engine change.
Environment: local machine. Unit and page tests under jsdom; browser checks
against the local stack — the engine on 8787, the server, and Vite — with a
guest session and synthetic play.
Prior full review: none; the prior change review is
[2026-09-22-219c1eb-change.md](2026-09-22-219c1eb-change.md).
Overall result: no issues found in the tested scope.
Release recommendation: not applicable (no deployment yet).

## Coverage

This change draws things the table already received; it adds no data to any
view and takes no decision. Selected: SEC-01, SEC-07, SEC-09, SEC-11, SEC-14
and SEC-18. SEC-02, SEC-05, SEC-08, SEC-10, SEC-13, SEC-16 and SEC-17 cover
surfaces nothing here touches: no route, socket event, authorization rule,
input, stored row or log line changes. SEC-03 through SEC-06, SEC-12, SEC-15,
SEC-19 through SEC-22 are outside this change's scope.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No route, socket event or handler added. The change is inside one glue module's `plan`, plus two presentation lines in the shared primitives | |
| SEC-07 | Hidden information | PASS (reproduced) | Everything the rail draws — `truthful_clues`, `truthful_values`, `negative_clues` — is in the engine's **public** view and was already sent to both seats; the rail only stops throwing it away. Asserted in `glue.test.ts`: the Detective's plan and the Hacker's produce an identical rail, which is what "public" means here. The map badges are the locations' printed facts, from public reference data both seats already fetch | |
| SEC-07 | Nothing new is derived | PASS | The table counts nothing and rules nothing in or out: it prints the tokens the engine published. The derived counts the artboards show ("three still standing") were deliberately left out — see "Left open: the counting" in the build notes | The question returns with the clue-giving form |
| SEC-09 | Game actions and agency | PASS | No move, template or lit part changes. The rail and the badges are not selectable, so nothing new can be submitted from them; `submissionLog` is untouched by the existing agency tests, which still pass | |
| SEC-11 | Browser content | PASS | Every string drawn comes from the engine's reference data or its public view, through React's own escaping; no `dangerouslySetInnerHTML`, no URL built from game text. A clue token id the reference data does not recognise draws nothing rather than falling through raw | |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned for local paths, addresses and token-like strings: none | |
| SEC-18 | Dependencies and supply chain | PASS | No dependency, script or lockfile change | |

## Findings

None.

## Execution

Commands/tools and versions: `npx tsc --noEmit` for the web app and the
primitives package; `npx vitest run --root apps/web` (vitest 2.1.9); two
throwaway Playwright runs against the local stack.
Tests passed/failed/skipped; real engine versus stub: 72 passed here, 0 failed,
0 skipped. The jsdom tests run against fixtures written to the engine's view
shape; the browser checks against the real engine, including a bought clue so
the rail was seen filled as well as empty, and a check that no raw id (`Gang 1`,
`Corp 2`, `gang_`, `the_hive`) reaches the board while the printed names do.
Browser/transport/configuration coverage: Chromium through Playwright at
1440×900 over HTTP and websockets, SQLite storage. The in-app browser pane was
not used.
Dependency and secret scan scope/date: 2026-09-22, the staged diff only; no
dependency change to scan.
Checks blocked/not run and exact reason: the Burn-the-Safehouse acceptance for
the rail was not reached — a Burn takes a long game — and is recorded as unmet
in the build notes rather than assumed. Postgres remains unexercised, as in
every prior report.
Prior findings rechecked, fixed, reopened, or still unverified: none; this
change touches none of their surfaces.
Files changed and temporary-resource cleanup: the six files in the staged diff.
Two throwaway browser specs and their screenshots live in a scratch directory
outside the repository and are not part of the suite. The dev stack started for
these checks was left running and must be stopped.
Next required review: full, at the first development session of the coming
week, and before accepting any milestone.
