# Security review — 2026-09-22

Mode: change (the Hacker's own view names their hideout, and the table reads
the name)
Commit and working-tree scope: the working tree on top of commit 219c1eb on
`main`, committed right after this report. Changed surfaces in this repository:
one glue module (`apps/web/src/glue/cybernoir-2127.ts`), its fixture tests, and
the implementation plan. The paired engine change is `e80c9ea` in the sibling
engine repository: the Cybernoir Hacker's player view gains `location_name`
beside the three printed facts it already carried.
Environment: local machine. Unit and page tests under jsdom with a fake socket.
The browser checks ran against the local review stack — the rebuilt engine on
8787, the server, and Vite — with synthetic accounts.
Prior full review: none; the prior change review is
[2026-09-22-95656c5-change.md](2026-09-22-95656c5-change.md), whose finding
SEC-09-F01 this pair of changes follows.
Overall result: no issues found in the tested scope.
Release recommendation: not applicable (no deployment yet).

## Coverage

This change adds a private fact to a seat view, so SEC-07 is the point of it.
Selected: SEC-01, SEC-07, SEC-10, SEC-14 and SEC-18. SEC-02, SEC-05, SEC-08,
SEC-09, SEC-13, SEC-16 and SEC-17 cover surfaces nothing here touches: no
route, socket event, authorization rule, stored row or log line changes, and
no move or decision changes hands. SEC-03 through SEC-06, SEC-11 through
SEC-13, SEC-15, SEC-19 through SEC-22 are outside this change's scope.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No route, socket event or handler added. One existing field in one game's player view gains a property; the browser side reads it | |
| SEC-07 | Who may see the hideout | PASS (reproduced) | The name is added inside the Hacker branch of the engine's `getPlayerView`. The Detective's view still has no `hideout` property at all and the public view has none; asserted against a real session in the engine's `digitalMode.test.ts` ("still tells the Detective nothing"), which also serializes both views and looks for the chosen location's name | |
| SEC-07 | The same, end to end | PASS (reproduced) | `e2e/cybernoir-two-browser.spec.ts` against the review stack: two signed-in humans, the host as Hacker choosing on the map, and the Detective's event feed carries no `"hideout":`, none of the Hacker's cards, and no legal moves that are not theirs | Ran once, against SQLite |
| SEC-07 | An AI Hacker's view | PASS | An AI seat's view never leaves the engine on a `run_ai_turn` response: `snapshotSeats` returns only `human` seats with `table: 'digital'` that `verifySeatAuth` accepts for the caller's token, and `get_state` verifies the seat's token before returning a player view. Read at both call sites | Inspection, not a reproduction; the engine's own suite covers the seat-auth rule |
| SEC-07 | What the table draws | PASS (reproduced) | The glue marks the safehouse only for `role === 'hacker'`, as before, and only when the view names a place. A view that gives the facts without a name marks nothing and prints the facts, rather than guessing between the two locations that share all three (`glue.test.ts`) | |
| SEC-10 | Validation | PASS | Read-only on the browser side: the name is drawn as text and compared against the reference data's location names to place the marker. An unknown name marks nothing | |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned for local paths, addresses and token-like strings: none. Fixtures use the game's own printed names | |
| SEC-18 | Dependencies and supply chain | PASS | No dependency, script or lockfile change | |

## Findings

None. [SEC-09-F01](2026-09-22-95656c5-change.md) from the prior report is
fixed: the engine auto-completes Cybernoir setup only when the AI plays the
Hacker, and a human Hacker reports their own. Verified against the live stack
in a browser — the setup phase opens with the city lit, and the game begins
only once the person presses through the sheet.

## Execution

Commands/tools and versions: `npx tsc --noEmit` in both repositories;
`npx vitest run --root apps/web` (vitest 2.1.9); `npx vitest run
src/games/cybernoir-2127` in the engine; `npx playwright test` against the
review stack.
Tests passed/failed/skipped; real engine versus stub: 70 passed here, 0 failed,
0 skipped; 257 passed in the engine's Cybernoir suite. The engine tests and the
browser specs run against the real engine; the jsdom tests against fixtures
written to the engine's view shape. The new engine tests were confirmed to go
red when the field is removed.
Browser/transport/configuration coverage: Chromium through Playwright at
1440×900, over HTTP and websockets to the review server, SQLite storage. The
in-app browser pane was used for inspection only — its synthetic clicks do not
reach the page while the pane is hidden.
Dependency and secret scan scope/date: 2026-09-22, the staged diff only; no
dependency change to scan.
Checks blocked/not run and exact reason: Postgres remains unexercised, as in
every prior report. The full engine suite was not re-run for this change; its
one known failure (Wonderland's War reporting its whole choice catalog missed)
is unrelated and was recorded in the prior report.
Prior findings rechecked, fixed, reopened, or still unverified: SEC-09-F01
fixed and verified as above.
Files changed and temporary-resource cleanup: the three files in the staged
diff, plus the engine commit named above. A throwaway browser spec and its
screenshots were written to a scratch directory outside the repository and are
not part of the suite; the review server's SQLite file is the usual untracked
`review.db`.
Next required review: full, at the first development session of the coming
week, and before accepting any milestone.
