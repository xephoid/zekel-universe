# Security review — 2026-09-22

Mode: change (items 5 and 6 of the Cybernoir table's design pass: the turn as
a row of verbs, and who the Detective can reach)
Commit and working-tree scope: the working tree on top of commit 578b0b4 on
`main`, committed right after this report. Changed surfaces: one glue module
(`apps/web/src/glue/cybernoir-2127.ts`), the glue contract (`types.ts`, one new
`PromptAction` variant), the table page's action handler (`Table.tsx`), the two
test files, three lines of primitive styles, and the implementation plan. No
server, wire, database, dependency, configuration or engine change.
Environment: local machine. Unit and page tests under jsdom with a fake socket;
browser checks against the local stack — the engine on 8787, the server, and
Vite — with a guest session, played as the Detective.
Prior full review: none; the prior change review is
[2026-09-22-565c12c-change.md](2026-09-22-565c12c-change.md).
Overall result: no issues found in the tested scope.
Release recommendation: not applicable (no deployment yet).

## Coverage

This change adds a second route by which a move can be submitted — an
action-bar button standing for several moves — so SEC-09 is the point of the
review. Selected: SEC-01, SEC-07, SEC-09, SEC-10, SEC-11, SEC-14 and SEC-18.
SEC-02, SEC-05, SEC-08, SEC-13, SEC-16 and SEC-17 cover surfaces nothing here
touches: no route, socket event, authorization rule, stored row or log line
changes. SEC-03 through SEC-06, SEC-12, SEC-15, SEC-19 through SEC-22 are
outside this change's scope.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No route, socket event or handler added. One branch added to the table page's action handler, and one optional variant to the glue contract | |
| SEC-09 | Agency: the new submission route | PASS (reproduced) | A verb carries only moves the engine listed, and carries them unchanged. Pressing one with several behind it opens the existing chooser and submits nothing; the pick goes out through `submitMove` with the `tap` trigger, which refuses anything not in the engine's list. Pressing one with a single move behind it still waits for the press. Both paths driven on the real page against a fake socket in `table-forms.test.tsx` ("cybernoir's verb bar"), asserting no `move` is emitted and `submissionLog` stays empty until the player picks | |
| SEC-09 | Agency: the reach list | PASS (reproduced) | A person is lit only when the engine lists a move naming them; a tap returns every listed move for that person and the chooser asks which, in the engine's own words. Nothing is preselected and no move is synthesised: `movesForSelect` filters `input.legalMoves` and never constructs a move (`glue.test.ts`). Checked on the live table: a tap offered an arrest and a recruitment and sent neither | |
| SEC-09 | No rule added | PASS | The verbs are a grouping of the engine's own move types, and a verb is drawn only where the engine lists something behind it. No cost, no legality and no ordering is computed here — the notes' "cost as note" was deliberately not done for that reason, and is written up in the build notes | |
| SEC-07 | Hidden information | PASS | The reach list is built from the played board (public), the reference data's residents (public, the printed cards), and — for whether a person is already spoken for — the jail and Evidence blocks of the public view plus `informants`, which exists only on the Detective's own view. Nothing is drawn from a seat's view that the seat does not hold, and no new field is read | The Hacker's plan has no reach list at all: it is built only in the Detective branch |
| SEC-10 | Validation | PASS | Read-only on the browser side. A person or location the reference data does not carry is drawn as a bare name; a move type no verb lists still appears in the numbered menu rather than being dropped | |
| SEC-11 | Browser content | PASS | Every string drawn comes from the engine's reference data, its view, or its own move descriptions, through React's own escaping. The long burn description is placed in a `title` attribute, not as markup | |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned for local paths, addresses and token-like strings: none | |
| SEC-18 | Dependencies and supply chain | PASS | No dependency, script or lockfile change | |

## Findings

None.

## Execution

Commands/tools and versions: `npx tsc --noEmit` for the web app and the
primitives package; `npx vitest run --root apps/web` (vitest 2.1.9); a
throwaway Playwright run against the local stack.
Tests passed/failed/skipped; real engine versus stub: 81 passed, 0 failed, 0
skipped. The two new page tests drive the real table page with a fake socket;
the browser run used the real engine and asserted the verb bar's labels, that
no verb label is a paragraph, that the reach list lights, and that a tap on a
person opens the chooser with both prices.
Browser/transport/configuration coverage: Chromium through Playwright at
1440×900 over HTTP and websockets, SQLite storage.
Dependency and secret scan scope/date: 2026-09-22, the staged diff only; no
dependency change to scan.
Checks blocked/not run and exact reason: item 6's third acceptance point — a
person already played as Evidence or already an informant drawn out of reach —
is covered in fixtures but was not seen live, because reaching that state needs
a longer game than these checks play; recorded as unseen in the build notes
rather than assumed. Postgres remains unexercised.
Prior findings rechecked, fixed, reopened, or still unverified: none; this
change touches none of their surfaces.
Files changed and temporary-resource cleanup: the seven files in the staged
diff. Throwaway browser specs and screenshots live in a scratch directory
outside the repository. The dev stack started for these checks was stopped
afterwards.
Next required review: full, at the first development session of the coming
week, and before accepting any milestone.
