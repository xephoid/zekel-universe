# Security review — 2026-09-21

Mode: change (the My tables page with table deletion, and the game's own
setup choices on the setup screen applied as setup moves)
Commit and working-tree scope: the working tree on top of commit 64bfd4d on
`main`, committed in pieces right after this report. Changed surfaces: the
server (`apps/server/src/{app.ts,tables/service.ts,db/index.ts,db/schema.ts}`,
tests), the wire contract, the web glue contract and three glue modules,
the setup page, the new My tables page, the bar's link, the API client, and
the browser specs.
Environment: local machine, synthetic fixtures only; unit tests against the
scripted fake engine and in-memory SQLite; the browser specs against the
local review stack (sibling engine, server on a local SQLite file, Vite).
Prior full review: none; the prior change review is
[2026-09-17-bc0fee0-change.md](2026-09-17-bc0fee0-change.md).
Overall result: no issues found in the tested scope; the earlier findings
still open.
Release recommendation: not applicable (no deployment yet).

## Coverage

Selected under change mode: SEC-01, SEC-02, SEC-14 and SEC-18 (always),
the "tables, sockets, moves, events, replay, game glue" row (SEC-02,
SEC-05, SEC-07 through SEC-10, SEC-13, SEC-16, SEC-17) and the "database,
migrations" row (SEC-13, SEC-19). SEC-03, SEC-04, SEC-06, SEC-11, SEC-12,
SEC-15, SEC-20 through SEC-22 are outside this change's scope and were not
run; their code did not change.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | One new route, `DELETE /api/tables/:id`, under the non-GET Origin hook and the principal check; `POST /api/tables` takes a new optional `setupMoves` list. Inventory below | |
| SEC-02 | Authorization on delete | PASS (reproduced) | Only the host may delete, and only while no other person holds a seat; anyone else is 403, a lobby a friend has joined is 403 for the host, a deleted table is 404 after (`rest.test.ts`). The summary's `deletable` flag is computed by the same rule, so the page never offers what the server refuses. A delete removes the table's events, seats, invites and notifications, so no row survives that could name the table | The engine keeps its session; the row that reached it is gone. A session-delete on the engine would tidy storage (not a security concern) |
| SEC-05 | Origins | PASS | The new route is a DELETE, covered by the existing non-GET Origin check; the spec drives it from the app's own origin | A missing Origin is still allowed for non-browser clients by design |
| SEC-07 | Hidden information | PASS | Setup moves are applied before the opening events, so the first event already reflects them; per-seat payloads are built as before. A watcher of a deleted table gets 404 (`storefront.spec.ts`) | |
| SEC-09 | Game actions and agency | PASS (reproduced) | Setup moves are the host's own choices from the setup screen, sent by the host's press of Start: nothing is preselected, the Start button waits until every listed choice is answered, and two seats sharing a faction are refused on the page before the engine would refuse them (`sweetlands.spec.ts`, `warble-way.spec.ts`). The server applies them by the host's seat only, in order, and the engine validates each; a refusal fails the creation with the engine's reason (422) and leaves no table behind (`rest.test.ts`, `move-flow.test.ts`). Choices that belong to a friend not at the setup screen are not listed (`glue.test.ts`: a friend's seat removes the faction fields). Randomness stays on the Roll and Draw buttons | The host still assigns the AI seats' factions, as the engine asks the first human to |
| SEC-09 (addendum) | Cybernoir's role choice | PASS (reproduced) | The host picks Detective or Hacker on the setup screen; the glue maps it to the engine's `detective` option by seat position, never a guess when unanswered (`glue.test.ts`). In two browsers, the host as Hacker sees only the Hacker's hand and the friend as Detective only the location hand (`cybernoir-two-browser.spec.ts`) | The host picks for a two-human table; the friend takes the other role |
| SEC-14 (addendum) | The tab icon | PASS | Three static files under `apps/web/public` (an SVG drawn by hand from the wordmark's five shapes, and two PNGs rendered from it by headless Chromium): no metadata, no text beyond the SVG's own comment, no new entry point, no dependency change. `index.html` links them by absolute path on the app's own origin (`storefront.spec.ts` fetches the SVG and the PNG) | |
| SEC-10 | Validation | PASS | `setupMoves` must be a list of at most 20 plain objects (400 otherwise); each move goes to the engine as a move for the host's seat and is validated there. Setup answers are typed on the page (choice, exactly-N picks, text with a length cap, numbers in range) | |
| SEC-13 | Persistence and migration | PASS (SQLite) / BLOCKED (Postgres) | Schema version 4 adds `tables.setup_moves`; a version 3 database migrates in place through the same mechanism the version 3 step used (`db.test.ts` exercises the path) | Postgres still not run here |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned for local paths, addresses and token-like strings: none | |
| SEC-16 | Abuse and cost | PASS | Setup moves are capped at 20 per creation, each one engine call, under the existing per-principal table budget; a delete is a handful of local deletes | |
| SEC-17 | Logs | PASS | No new logging | |
| SEC-18 | Dependencies, CI | PASS with note | No dependency change | F01 unchanged |

## Findings

No new findings.

### Earlier findings

- SEC-18-F01, SEC-08-F02, SEC-04-F03, SEC-15-F07, SEC-16-F08: open, unchanged.

## Execution

Commands/tools and versions: Node 24 locally (CI uses 22), pnpm 10, vitest
2, Playwright 1.63 with Chromium, the sibling engine at its current commit.
Tests passed/failed/skipped; real engine versus stub: server 9 files passed
(new: setup moves applied and refused, delete rules and cascade); web 6
files passed (new: the setup fields and setup moves of Sweetlands and
Warble Way); engine-client 2 files passed; typecheck and the production
build clean. Browser specs against the live engine: the four changed specs
passed; the full run of eight passed seven with the Fractured Fist game
failing on a menu race while a screenshot script played a second game on
the same server, and passed alone right after.
Browser/transport/configuration coverage: Chromium through Playwright;
development configuration.
Dependency and secret scan scope/date: no dependency change; changed files
scanned on 2026-09-21.
Checks blocked/not run and exact reason: Postgres and compose (Docker
unavailable); the domains listed above as outside scope.
Prior findings rechecked, fixed, reopened, or still unverified: all
rechecked against the diff, still open, surfaces unchanged.
Files changed and temporary-resource cleanup: the review stack's SQLite
file is ignored by git and deleted after the run.
Next required review: a full review at the first development session of the
next active week (still due), and a release review before the first deploy.

## Entry-point inventory added at this revision

REST: `DELETE /api/tables/:id`; `POST /api/tables` gains `setupMoves`.
Static: `/favicon.svg`, `/favicon-32.png`, `/apple-touch-icon.png` (files
in the web build, served like the rest of it).
Socket and outbound: unchanged.
