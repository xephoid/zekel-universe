# Security review — 2026-09-22

Mode: change (the city settled as a map: labelled areas and pill regions)
Commit and working-tree scope: the working tree on top of commit e0f0bd9 on
`main`, committed right after this report. Changed surfaces: two additive
fields on the map primitive and their rendering
(`packages/primitives/src/{types,components,primitives.css}`), the Cybernoir
glue's map build, and its fixture tests. No server, wire, database,
dependency, configuration or engine change.
Environment: local machine. Unit tests under jsdom; browser checks against the
local stack — the engine on 8787, the server, and Vite — with a guest session,
across three games.
Prior full review: none; the prior change review is
[2026-09-22-d6b74c8-change.md](2026-09-22-d6b74c8-change.md).
Overall result: no issues found in the tested scope.
Release recommendation: not applicable (no deployment yet).

## Coverage

This is layout and labelling on data the table already drew. Selected:
SEC-01, SEC-07, SEC-09, SEC-10, SEC-11, SEC-14 and SEC-18. Every other domain
covers surfaces nothing here touches.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No route, socket event or handler. Two optional fields on `MapData`, one on `MapNode`, and the glue that fills them | |
| SEC-07 | Hidden information | PASS (reproduced) | A band's label is a borough's printed name and its count is how many of the nineteen printed locations sit in it — public reference data both seats fetch. A region's colour is its printed faction, also public. The safehouse mark is unchanged: still drawn only for its owner, and only when their own view names the place (`glue.test.ts`, unchanged and passing) | |
| SEC-07 | What moved off the badges | PASS | Nothing was added or removed from the view; three facts that were drawn as text badges are now drawn as a band, a colour and one badge. The same public facts, said once each | |
| SEC-09 | Game actions and agency | PASS | Lighting and selection are untouched: a region lights when the engine lists a move naming it and a tap means that move. A band is drawn with `pointer-events: none`, so it cannot be clicked at all. The 92 tests including the agency ones pass | |
| SEC-10 | Validation | PASS | A map with no `areas` draws no bands and a node with no `area` sits where its coordinates put it, so every other board is unaffected by construction. Confirmed on Sweetlands' eighty-space map, which is unchanged | |
| SEC-11 | Browser content | PASS | Labels and titles are game text through React's own escaping. The long-name `title` attribute carries the location's printed name, which is public | |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned for local paths, addresses and token-like strings: none | |
| SEC-18 | Dependencies and supply chain | PASS | No dependency, script or lockfile change | |

## Findings

None.

## Execution

Commands/tools and versions: `npx tsc --noEmit` for the web app and the
primitives package; `npx vitest run --root apps/web` (vitest 2.1.9); one
throwaway Playwright run opening a table in three games.
Tests passed/failed/skipped; real engine versus stub: 92 passed, 0 failed, 0
skipped. The browser run opened Cybernoir, Fractured Fist and Sweetlands
Imperium against the real engine: the city now reads as three labelled borough
bands of nineteen named regions with nothing overlapping, and the other two
games are unchanged.
Browser/transport/configuration coverage: Chromium through Playwright at
1440×900 over HTTP and websockets, SQLite storage.
Dependency and secret scan scope/date: 2026-09-22, the staged diff only.
Checks blocked/not run and exact reason: the component sheet's "crossed off"
region state cannot be drawn at all — the engine keeps `seen_locations` in
state and does not publish it — and is recorded in the build notes as a fourth
ask of the engine rather than left silent. Postgres remains unexercised.
Prior findings rechecked, fixed, reopened, or still unverified: none; this
change touches none of their surfaces.
Files changed and temporary-resource cleanup: the six files in the staged
diff. Throwaway browser specs and screenshots live in a scratch directory
outside the repository. The dev stack started for these checks was stopped
afterwards.
Next required review: full, at the first development session of the coming
week, and before accepting any milestone.
