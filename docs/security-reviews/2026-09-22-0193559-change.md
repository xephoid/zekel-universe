# Security review — 2026-09-22

Mode: change (the case file carries what is printed on the Evidence, a small
chip stops hiding a card's facts, and the action bar is never empty while the
engine is waiting)
Commit and working-tree scope: the working tree on top of commit 0193559 on
`main`, committed right after this report. Changed surfaces: the Cybernoir
glue's case file and verb list, its fixture tests, and the small-zone rules in
`packages/primitives/src/primitives.css`. No type, server, wire, database,
dependency, configuration or engine change.
Environment: local machine. Unit tests under jsdom; browser checks against the
local stack — the engine on 8787, the server, and Vite — with a guest session,
played as the Hacker until a Motive set reached the table.
Prior full review: none; the prior change review is
[2026-09-22-1ea3150-change.md](2026-09-22-1ea3150-change.md).
Overall result: no issues found in the tested scope. The central question here
is whether showing more on the Evidence tells anyone something they are not
entitled to; it does not, and the reasoning is recorded under SEC-07.
Release recommendation: not applicable (no deployment yet).

## Coverage

This shows more of what is already public and adds a button. Selected: SEC-01,
SEC-07, SEC-09, SEC-10, SEC-11, SEC-14 and SEC-18.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-07 | Is the Evidence public? | PASS (reasoned and reproduced) | Yes, by the rules and by the engine: the Evidence zone is face up on the table and sits in the **public** view (`getPublicView`'s `evidence` block), so both seats already received every name on it. What is added beside each name — where that person lives, their faction, their cost — is printed on the card and comes from `reference_data.people`, which both seats fetch. Nothing hidden is read, and the Hacker's hand, deck and hideout are untouched | |
| SEC-07 | Does it help the Detective too much? | PASS (by design, and not a disclosure) | It helps, and that is the point: a played Motive set read as three people all living at one location, which was the safehouse. But every one of those facts was already on the table — the names were public and the residents of all nineteen locations are printed. The table is showing a deduction the player could make with the cards in front of them, not telling them something the engine kept | Universe still derives nothing: it prints each card's own facts and draws no conclusion from them |
| SEC-07 | The chip that stopped hiding | PASS | A small zone now renders the subtitle and badges its data already carried. The only zones affected are Cybernoir's case file and "Who you can reach", whose subtitles are public facts or the Detective's own ("your informant") and were set by the glue only in that seat's plan | |
| SEC-09 | Agency: the fallback verb | PASS (reproduced) | It carries the engine's listed moves for a type no verb claimed, unchanged, through the same path as every other verb: one sends on the press, several open the chooser. It cannot offer a move the engine did not list (`glue.test.ts`). It removes a dead bar; it does not widen what can be sent | |
| SEC-01 | Entry points | PASS | No route, socket event, handler or contract change | |
| SEC-10 | Validation | PASS | A name the reference data does not carry draws as itself, without facts; a move type with no words draws the engine's own type in words | |
| SEC-11 | Browser content | PASS | Every string is game text through React's own escaping | |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned for local paths, addresses and token-like strings: none | |
| SEC-18 | Dependencies and supply chain | PASS | No dependency, script or lockfile change | |

## Findings

None. One usability hole was closed rather than a security one: the action bar
drew nothing when the engine's pending step was a move type no verb claimed,
which is when an answer was owed. It blocked three live checks in this session
before being fixed, which is how it was found.

## Execution

Commands/tools and versions: `npx tsc --noEmit` for the web app and the
primitives package; `npx vitest run --root apps/web` (vitest 2.1.9); several
throwaway Playwright runs against the local stack.
Tests passed/failed/skipped; real engine versus stub: 95 passed, 0 failed, 0
skipped. The browser run used the real engine and played a complete Motive set
onto the table; the case file then read three named cards each carrying where
that person lives, their faction and their cost.
Browser/transport/configuration coverage: Chromium through Playwright at
1440×900 over HTTP and websockets, SQLite storage.
Dependency and secret scan scope/date: 2026-09-22, the staged diff only.
Checks blocked/not run and exact reason: the Detective's own view of a filled
case file was covered in fixtures rather than live — reaching it needs the AI
Hacker to play Evidence — and both seats render from the same code. Postgres
remains unexercised.
Prior findings rechecked, fixed, reopened, or still unverified: none; this
change touches none of their surfaces.
Files changed and temporary-resource cleanup: the four files in the staged
diff. Throwaway browser specs and screenshots live in a scratch directory
outside the repository. The dev stack started for these checks was stopped
afterwards.
Next required review: full, at the first development session of the coming
week, and before accepting any milestone.
