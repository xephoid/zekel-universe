# Security review — 2026-09-24

Mode: change (the engine's reasons for blocked moves, passed from the
server to the deciding seat and shown on the NGnG purchase screens)
Commit and working-tree scope: the working tree on `main` over `69b53b8`,
committed right after this report. Changed surfaces: shared types
(`UnavailableMove`, `SeatPayload.unavailable`, `TableEventWire.unavailable`),
the server's seat payloads and wire events, the fake engine, the table page,
the NGnG purchase screens, the fixture capture script, tests.
Environment: local machine. Server tests against the in-process server with
the fake engine; page tests under jsdom; a live browser run against the
local engine (6864d16), the restarted server and Vite.
Prior full review: none. The latest change review is
[2026-09-24-634d53d-change.md](2026-09-24-634d53d-change.md). A full review
is still due.
Overall result: no findings.
Release recommendation: not applicable (no deployment yet).

## Coverage

Engine data now reaches browsers on a new field, so SEC-07, SEC-10 and
SEC-11 carry the weight. Selected: SEC-01, SEC-02, SEC-07, SEC-09, SEC-10,
SEC-11, SEC-14, SEC-18.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No new route or socket event; an existing event carries one more field | |
| SEC-02 | Authorization | PASS | The list is fetched with the seat's own legal moves, only on that seat's turn, and stored on that seat's payload | |
| SEC-07 | Hidden information | PASS | `toWireEvent` sends a seat only its own payload. `socket-privacy.test.ts`: the seat on turn gets its reasons; the other seat's event has none and does not contain them. The engine builds the list from the seat's own position | |
| SEC-09 | Agency | PASS | A struck line is not a button and cannot be picked; nothing is sent from it (`ngg-economy.test.tsx`) | |
| SEC-10 | Validation | PASS | `unavailableOf` keeps at most 100 entries, each needing a string move type (64 characters) and reason (400), with an optional item (120); other shapes are dropped (tested with malformed and oversized entries) | |
| SEC-11 | Browser content | PASS | Reasons render as text through React's escaping; no raw HTML | |
| SEC-14 | Secrets and artifacts | PASS | Diff searched for credentials and local paths: none | |
| SEC-18 | Dependencies | PASS | No dependency or lockfile change | |

## Execution

Commands/tools and versions: `npx tsc --noEmit` for web, server and shared;
`npx vitest run` in `apps/web` (313 passed) and `apps/server` (71 passed,
1 skipped) (vitest 2.1.9); a live Playwright (Chromium) run to a Research
decision, where the blocked technologies and the battle card showed with
the engine's reasons.
Dependency and secret scan scope/date: 2026-09-24, the working-tree diff.
