# Security review — 2026-09-17 (M4)

Mode: change (M4, two more games: the Sweetlands and Warble Way glue played
through, forms for template moves, choosers, the Roll and Draw buttons,
the Sweetlands art, and three small engine changes in the sibling repository)
Commit and working-tree scope: the working tree on top of commit 8b91751 on
`main`, committed in pieces right after this report. Changed surfaces: the
primitives (art fields, a hidden-document guard in the motion root), the
web glue contract and agency policy (`apps/web/src/glue/{types,agency,forms}.ts`),
the Sweetlands and Warble Way glue, the table page and its parts (chooser
and form sheets), the menu's row-to-move matching, layout CSS, tests, two
browser specs, the README and the plan. In the engine repository: one commit
touching `sweetlands-imperium/{engine,legalMoves}.ts` and
`warble-way-galaxy/index.ts`. No server code changed in this repository.
Environment: local machine, synthetic fixtures only. Unit tests against
fixture views; the browser specs against a local review stack (the rebuilt
sibling engine, the server on a local SQLite file, the Vite dev server), a
guest seat, no real users, no production system.
Prior full review: none; the prior change review is
[2026-09-17-926c668-change.md](2026-09-17-926c668-change.md).
Overall result: no issues found in the tested scope; one design limitation
recorded in the plan (faction assignment at multi-human tables), the three
earlier findings and F07 still open.
Release recommendation: not applicable (no deployment yet).

## Coverage

Selected under change mode: SEC-01, SEC-02, SEC-14 and SEC-18 (always),
the "tables, sockets, moves, events, replay, game glue" row (SEC-02,
SEC-05, SEC-07 through SEC-10, SEC-13, SEC-16, SEC-17), the "engine
client or engine deployment" row for the engine changes (SEC-09 through
SEC-12), and the "React, content, images" row (SEC-07, SEC-10 through
SEC-12, SEC-15, SEC-20). SEC-03, SEC-04, SEC-06, SEC-19, SEC-21 and SEC-22
are outside this change's scope and were not run; their code did not change.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No new route, socket message or worker in this repository. In the engine, no new tool; two existing games answer differently for digital seats | |
| SEC-02 | Authorization | PASS | Unchanged: every move still goes through the socket's seat check and the engine's own validation; a form-built move is validated by the engine exactly like a menu move | |
| SEC-05 | Origins | NOT RUN | No change to origins or cookies | |
| SEC-07 | Hidden information | PASS | The Sweetlands view is the engine's per-seat view; the glue draws `your_hand`, `your_secret_objectives` and `your_turn_step` only for the owning seat (fixture test: the AI seat's tableau shows a hand count, never cards). The chooser and forms show only that seat's legal moves. Warble Way is solo | The two-human Sweetlands table was not driven in two browsers (the Cybernoir spec covers the socket property; the glue reads the same per-seat payload) |
| SEC-09 | Game actions, agency, replay | PASS (reproduced) | The agency policy gains one trigger, `form`: allowed only for a completion of a listed template where the answers changed just the keys the form asked about and invented none (`agency.test.ts`). The table page sends nothing while a chooser or form is open, closes both on a new event, and sends a form only on its button (`table-forms.test.tsx`, three tests). In the browser: the Sweetlands faction form starts empty and its button waits (`sweetlands.spec.ts`), the Warble Way character form likewise (`warble-way.spec.ts`). Randomness: the Intel draw, the travel card and every die wait for the Draw or Roll press (both specs); the foe battle on a digital seat is rolled by the engine when the player presses the move that starts it (engine `digitalMode.test.ts`: no `battleTotal` to fill in; a physical seat still must declare its roll) | The menu now matches a row to a move by description when the engine lists no ids; Sweetlands now lists ids, so this fallback is for other games |
| SEC-10 | Validation | PASS | Form answers are trimmed strings, bounded numbers or picks from listed options; the glue builds the move and the engine validates it (a wrong faction map or score spread is refused by the engine, shown as a rule). `templateForm` fills only empty-string blanks with text of at most 60 characters | |
| SEC-11 | Browser content | PASS | The reference data's names, descriptions and archetype labels render as text through React; no HTML is built from them. Art is served from the app's own origin (`/art/sweetlands/*`, already present) | |
| SEC-12 | Outgoing requests | PASS | None added | |
| SEC-13 | Persistence | NOT RUN | No schema or event change | |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned for local paths, addresses and token-like strings: none. The art is Zekel Games' own (the four factions, the Nomads and the treats), placed per CLAUDE.md; no third-party content added | |
| SEC-15 | Privacy | PASS | Nothing new leaves the table | |
| SEC-16 | Abuse and cost | PASS | A chooser or form adds no request until the player sends; one move per send as before | |
| SEC-17 | Logs | PASS | No new logging | |
| SEC-18 | Dependencies, CI | PASS with note | No dependency change. CI gains the two specs through the existing test-match. The engine's own tests pass with the change (23 files for the two games) | F01 unchanged |
| SEC-20 | UI integration | PASS (reproduced) | The board map is sized to the board area so every space is on screen above the bench (the first live run found the castle covered by the bench and the map at a few dozen pixels wide); the Draw button no longer collides with the map. The space-for-space check of the 80 spaces, 4 starts and the castle against the reference document runs in the unit suite | The built-in browser pane cannot verify motion (no animation frames while hidden); Playwright runs paint, and the specs passed there |

Regression recipes: R1, R2, R5, R6 and R7 unchanged in the unit suite; the
agency property (nothing submits without a person) now covers choosers and
forms in `table-forms.test.tsx`.

## Findings

No new findings.

### Earlier findings

- SEC-18-F01 (actions pinned by tag): open, unchanged.
- SEC-08-F02 (no socket message rate limit or idempotency key): open, unchanged.
- SEC-04-F03 (no session rotation; local snapshot survives sign-out): open, unchanged.
- SEC-15-F07 (the nudge names the game): open, awaiting a line in the brief.

## Execution

Commands/tools and versions: Node 24 locally (CI uses 22), pnpm 10, vitest
2, Playwright 1.63 with Chromium, the engine rebuilt from the sibling
checkout at its new commit; a regular-expression scan over the changed files.
Tests passed/failed/skipped; real engine versus stub: server 65 passed and
1 skipped; web 47 passed (glue 19, agency 7, table 6, table forms 3, queue
7, identity 5); engine-client 7 passed and 5 skipped; typecheck and the
production build clean; the engine's two game suites 23 files passed.
Browser specs against the live engine: Sweetlands (44 seconds) and Warble
Way (14 seconds) passed alone; the full run of all seven tests is recorded
in the commit that carries this report.
Browser/transport/configuration coverage: Chromium through Playwright; the
built-in browser for DOM inspection only; development configuration.
Dependency and secret scan scope/date: no dependency change; changed files
scanned on 2026-09-17.
Checks blocked/not run and exact reason: Postgres and compose (Docker
unavailable); R4, R9, R10; the domains listed above as outside scope; a
two-human Sweetlands table in two browsers (not written this pass).
Prior findings rechecked, fixed, reopened, or still unverified: F01 to F03
and F07 rechecked against the diff, still open, surfaces unchanged.
Files changed and temporary-resource cleanup: the review stack's SQLite
file is ignored by git and deleted after the run.
Next required review: a full review at the first development session of the
next active week (still due), and a release review before the first deploy.
