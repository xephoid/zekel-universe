# Security review — 2026-09-24

Mode: change (Neither Guts nor Gears table checked against engine 6864d16:
face-down planned cards, owned collectors, Cores in collectors, the new
initiative-tie move; test views recaptured)
Commit and working-tree scope: the working tree on `main` over `634d53d`,
committed right after this report. Changed surfaces: the NGnG reader,
screen context, faction board, round and battle screens, recaptured
fixtures, page tests and the NGnG end-to-end test. No server, socket,
dependency or configuration change.
Environment: local machine. Page tests under jsdom; the NGnG end-to-end
test against the local engine (rebuilt at 6864d16 and restarted), server
and Vite, once as a wizard seat and once as a robot seat.
Prior full review: none. The latest change review is
[2026-09-24-821db9f-change.md](2026-09-24-821db9f-change.md). A full review
is still due.
Overall result: no findings.
Release recommendation: not applicable (no deployment yet).

## Coverage

Selected: SEC-01, SEC-02, SEC-07, SEC-09, SEC-11, SEC-14, SEC-18.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No route, socket event or server code changed | |
| SEC-02 | Authorization | PASS | Moves still go through `submitMove` on the existing path | |
| SEC-07 | Hidden information | PASS | The engine now hides other seats' planned cards; the screen shows another seat only a face-down count and puts the seat's own private list only on its own seat (`ngg-screen.test.tsx`) | |
| SEC-09 | Agency | PASS | The tie is the engine's listed move, sent on a press; Core upkeep starts from the current placement and sends only on a press (tested). Both live runs played 120 decisions through the screens with no refusal | |
| SEC-11 | Browser content | PASS | No raw HTML | |
| SEC-14 | Secrets and artifacts | PASS | Diff searched for credentials and local paths: none. The engine token was read from the untracked `.env` only to restart the engine | |
| SEC-18 | Dependencies | PASS | No dependency or lockfile change | |

## Execution

Commands/tools and versions: engine `pnpm build` at 6864d16; `npx tsc
--noEmit` and `npx vitest run` in `apps/web` (310 passed, vitest 2.1.9);
Playwright (Chromium) `e2e/ngg.spec.ts` as a wizard seat (passed) and a
copy as a robot seat (passed, not committed).
Dependency and secret scan scope/date: 2026-09-24, the working-tree diff.
