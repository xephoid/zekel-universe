# Security review — 2026-09-24

Mode: change (Neither Guts nor Gears table fixes after play: a page theme
from the glue, the move list and faction strip placed by the screen, map
zoom and pan)
Commit and working-tree scope: the working tree on `main` over `5203f95`,
committed right after this report. Changed surfaces: `GlueModule.themeFor`
and two optional `GameScreenProps` fields (`menu`, `benchSlot`), the table
page that supplies them, the NGnG layout, map and stylesheet, and tests. No
server, socket, engine, dependency or configuration change.
Environment: local machine. Page tests under jsdom; a live browser run
against the local engine, server and Vite.
Prior full review: none. The latest change review is
[2026-09-24-36c84a5-change.md](2026-09-24-36c84a5-change.md). A full review
is still due.
Overall result: no findings.
Release recommendation: not applicable (no deployment yet).

## Coverage

A React-only change to the table page and one game's glue. Selected:
SEC-01, SEC-02, SEC-07, SEC-09, SEC-11, SEC-14, SEC-18.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No route, socket event or server code changed | |
| SEC-02 | Authorization | PASS | Nothing new reaches the server; moves still go through `submitMove` on the existing path | |
| SEC-07 | Hidden information | PASS | `themeFor` reads only the viewing seat's own faction from that seat's view; factions are public | |
| SEC-09 | Agency | PASS | The move list is the same `MoveMenuList` with the same `pick` handler, only placed in the column. A drag on the zoomed map suppresses the click that ends it, so a pan never presses a hex; zoom and pan send nothing (`ngg-screen.test.tsx`) | |
| SEC-11 | Browser content | PASS | No raw HTML; the bench is a React portal into the page's own element; the theme is a class name from a fixed set | |
| SEC-14 | Secrets and artifacts | PASS | Diff searched for credentials and local paths: none. Screenshots and the temporary Playwright spec stayed outside the repository | |
| SEC-18 | Dependencies | PASS | No dependency or lockfile change | |

## Execution

Commands/tools and versions: `npx tsc --noEmit` and `npx vitest run` in
`apps/web` (299 passed, vitest 2.1.9); Playwright (Chromium) screenshots of
a live table for a wizard seat and a robot seat, light and dark, at 1440×900.
Dependency and secret scan scope/date: 2026-09-24, the working-tree diff.
