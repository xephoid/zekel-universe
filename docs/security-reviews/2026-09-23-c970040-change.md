# Security review — 2026-09-23

Mode: change (teaching made small and contextual, large combination lists
turned into guided choices, and the map made readable for deduction)
Commit and working-tree scope: the working tree on top of commit c970040 on
`main`, committed right after this report. Changed surfaces: the glue contract
(`MoveForm.fieldsFor`, `GlueModule.detailFor`), the Cybernoir glue, the table
page (lesson queue, a detail sheet, persisted dismissals), the shared
primitives (three optional `MapNode` fields, two optional `MapData` fields),
styles and tests. No server, wire, database, dependency or engine change.
Environment: local machine. Unit and page tests under jsdom; browser checks
against the local stack, played as the Hacker.
Prior full review: none; the prior change review is
[2026-09-23-e4bcfe3-change.md](2026-09-23-e4bcfe3-change.md).
Overall result: no issues found in the tested scope.
Release recommendation: not applicable (no deployment yet).

## Coverage

Two things here deserve the weight: a new browser-storage write, and a new
way to reach information about a part. Selected: SEC-01, SEC-07, SEC-09,
SEC-10, SEC-11, SEC-14, SEC-15 (privacy and retention, for what is now kept
in the browser) and SEC-18.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-15 | What is now stored in the browser | PASS | Dismissed lesson ids, per game, under `universe:lessons:seen:<gameId>` — engine section ids such as `jail-track`, and nothing else. No table id, no seat, no player id, no game state, nothing about another person. Reads and writes are wrapped so a private window or blocked storage degrades to showing the lesson again rather than throwing | It is per browser, not per account, which is the right scope for "I have read this" |
| SEC-07 | The detail sheet | PASS (reproduced) | It reports a location's printed facts — borough, residents, faction — from public reference data, plus three states read from the seat's own view: played (public), in the Detective's discard (public), and "this is your safehouse", which is only ever produced when the seat's own view names it, the same gate as the map marker. A Detective tapping the hideout learns nothing: their view carries no hideout at all. Asserted in `glue.test.ts` | |
| SEC-07 | The discard piles | PASS | Both are public by the rules and already in the public view; they were simply not drawn. The Detective's discarded Locations come from `detective.location_discard`, the Hacker's spent Contacts from `contacts_discard` | |
| SEC-07 | The guided choices | PASS | Every step's options are read off the engine's own listed moves for this seat and narrowed by the previous answers. Nothing is enumerated that the engine did not already offer, so the sheet cannot reveal a person or token the seat was not already being offered | |
| SEC-09 | Agency: guided choices | PASS (reproduced) | Each step starts empty, a half-answered form builds nothing, and a combination the engine did not list cannot be sent — asserted for both the two-step jailbreak and the pick-a-set Evidence form. What goes out is the listed move, order and all | |
| SEC-09 | Agency: looking is not doing | PASS (reproduced) | A region with no move behind it is clickable, and the tap opens a sheet; `movesForSelect` returns nothing for it, so no submission path is reached. Checked live: tapping a quiet location opened its facts and Escape closed it | |
| SEC-01 | Entry points | PASS | No route, socket event or handler added | |
| SEC-10 | Validation | PASS | Every new field is optional and read defensively; a map with no legend or areas draws neither, and other games are untouched by construction | |
| SEC-11 | Browser content | PASS | Lesson text, location facts and option labels are engine text through React's escaping. The lesson's "first sentence" split is presentation only | |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned: none | |
| SEC-18 | Dependencies and supply chain | PASS | No dependency, script or lockfile change | |

## Findings

None.

## Execution

Commands/tools and versions: `npx tsc --noEmit` for the web app and the
primitives package; `npx vitest run --root apps/web` (vitest 2.1.9); a
throwaway Playwright run.
Tests passed/failed/skipped; real engine versus stub: 105 passed, 0 failed, 0
skipped. The browser run confirmed one lesson rather than six with the side
column's own panels intact, the colour legend, the clue rail directly under
the city, and a location's detail sheet opening on a tap with no move behind
it.
Browser/transport/configuration coverage: Chromium through Playwright at
1440×900 over HTTP and websockets, SQLite storage.
Dependency and secret scan scope/date: 2026-09-23, the staged diff only.
Checks blocked/not run and exact reason: ruled-out locations and the
Detective's crossed-off ones are not drawn at all — the engine computes both
and publishes neither, and that ask was not taken — so there was nothing to
check. Postgres remains unexercised.
Prior findings rechecked, fixed, reopened, or still unverified: none reopened.
Files changed and temporary-resource cleanup: the files in the staged diff.
Throwaway browser specs and screenshots live in a scratch directory outside
the repository. The dev stack was left running for the user.
Next required review: full, at the first development session of the coming
week, and before accepting any milestone.
