# Security review — 2026-09-23

Mode: change (the caption card holds the engine's narration to three lines,
with the whole of it a press away)
Commit and working-tree scope: the working tree on top of commit f891a4d on
`main`, committed right after this report. Three files: one new presentational
component in the table's parts, the table page's caption block and a sheet,
and two style rules. No server, wire, database, dependency, configuration,
primitive or engine change, and no new move path.
Environment: local machine. Unit and page tests under jsdom; a browser check
against the local stack — engine on 8787, server on 8790, Vite on 5173 —
played as the Hacker across fourteen turns at 1440×900.
Prior full review: none; the prior change review is
[2026-09-23-1e3cbed-change.md](2026-09-23-1e3cbed-change.md).
Overall result: no issues found in the tested scope.
Release recommendation: not applicable (no deployment yet).

## Coverage

Cutting text short is a disclosure question in reverse: the risk is not that
something is shown, but that something the player is entitled to read is taken
away, or that "the rest" opens somewhere it should not. SEC-07 and SEC-11
carry the weight. Selected: SEC-01, SEC-07, SEC-09, SEC-10, SEC-11, SEC-14,
SEC-18.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-07 | Nothing is withheld | PASS (reproduced) | The clamp is visual only: the whole narration stays in the DOM, so a screen reader reads all of it and nothing is removed from the page. Proved in the browser — with a 362-character narration cut to 57px of 94px, the text read back off the card is character-for-character the same string the sheet prints. The Log beside the table also carries every event in full, as it did before | |
| SEC-07 | Where the rest opens | PASS | "Read it all" shows the same `current.summary` string the card was already showing, in the existing sheet. No new source is read, nothing is fetched, and no other seat's text can reach it | |
| SEC-09 | Agency | PASS | No move, template, form or submission path changed. The new button opens a sheet and nothing else. The sheet is the existing focus-trapping one, so Tab cannot reach the table's own buttons behind it and spend action points by accident — the reason that trap exists. Escape closes it, checked live. The 108 tests including the agency ones pass | |
| SEC-11 | Browser content | PASS | The same engine string, rendered through React's escaping, in a `<p>` and a `<div>`. No markup is interpreted and no new text is composed | |
| SEC-10 | Validation | PASS | The component reads `scrollHeight` and `clientHeight` defensively and does nothing without an element; `ResizeObserver` is feature-checked, as elsewhere in this codebase. Where neither is available the text simply is not reported as cut, and the card shows what fits | In jsdom both heights are 0, so the button never appears there — see the limitation below |
| SEC-01 | Entry points | PASS | No route, socket event or handler added | |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned: none | |
| SEC-18 | Dependencies and supply chain | PASS | No dependency, script or lockfile change | |

## Findings

None.

## Execution

Commands/tools and versions: `npm run typecheck` across the workspace (all
clean); `npx vitest run --root apps/web` (vitest 2.1.9); a throwaway
Playwright spec playing fourteen turns and measuring the card on each.
Tests passed/failed/skipped; real engine versus stub: 108 passed, 0 failed, 0
skipped, against the real engine for the browser run.
Browser/transport/configuration coverage: Chromium through Playwright at
1440×900 over HTTP and websockets, SQLite storage. Narrations from 14 to 896
characters came up across the fourteen turns; the card measured 85px when the
text was short and 123px whenever it was long, and never more. Before this
change the same card reached 217px. When cut it drew exactly three lines, and
the sheet showed the same string whole.
Dependency and secret scan scope/date: 2026-09-23, the staged diff only.
Checks blocked/not run and exact reason: the clamp cannot be tested under
jsdom, which does no layout and reports every height as 0, so there is no unit
test for it and the guarantee rests on the browser run above. The card's
height is bounded but not constant — it moves between 85px and 123px with the
length of the text — so a small shift between moves remains, much smaller than
the one this change removes. The clue rail and the jail are still below the
fold at 1440×900: the board area is 473px and now holds 665px, the city taking
306 of it at its floor, the band 141 and the move menu 42. Closing that needs
room taken from the action bar, which is 146px in three rows, and that was not
taken here. Postgres remains unexercised.
Prior findings rechecked, fixed, reopened, or still unverified: none reopened.
The geometry checks from the previous change still pass.
Files changed and temporary-resource cleanup: the three files in the staged
diff. The throwaway specs, the temporary Playwright config and the screenshots
live in the session scratch directory outside the repository; the config was
deleted, and `git status` shows only the intended files plus
`docs/design/SCREEN-ROUTING.md`, which belongs to another session and another
project and was left alone. The dev stack was left running.
Next required review: full, at the first development session of the coming
week, and before accepting any milestone.
