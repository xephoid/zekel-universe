# Security review — 2026-09-22

Mode: change (a look pass: the game's faction colours, and every zone drawn
as a panel)
Commit and working-tree scope: the working tree on top of commit d6b74c8 on
`main`, committed right after this report. Changed surfaces: one glue module's
palette and colour keys (`apps/web/src/glue/cybernoir-2127.ts`), its fixture
tests, and one block of `apps/web/src/app.css`. No library, server, wire,
database, dependency, configuration or engine change.
Environment: local machine. Unit tests under jsdom; browser checks against the
local stack — the engine on 8787, the server, and Vite — with a guest session,
across three games.
Prior full review: none; the prior change review is
[2026-09-22-a68934a-change.md](2026-09-22-a68934a-change.md).
Overall result: no issues found in the tested scope.
Release recommendation: not applicable (no deployment yet).

## Coverage

This change is colour and chrome: it moves no data, adds no field, and submits
nothing. Selected: SEC-01, SEC-07, SEC-09, SEC-11, SEC-14 and SEC-18. Every
other domain covers surfaces nothing here touches.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No route, socket event, handler or contract change. A palette map, the colour keys the glue puts on parts, and one CSS rule | |
| SEC-07 | Hidden information | PASS | Colour is drawn from a part's own affiliation, which comes from the public reference data — the printed cards, which both seats already fetch. No view is read differently and no new field is read. In particular a face-down informant's colour is drawn only in the Detective's own plan, where the name was already drawn | |
| SEC-07 | What a colour could give away | PASS (reasoned) | A location's colour is its printed faction, which is on the card and public. It says nothing about the hideout: the safehouse mark is still drawn only for its owner and only when their own view names it, unchanged from the earlier review | |
| SEC-09 | Game actions and agency | PASS | No move, template, lit part or submission path changes; the 92 tests including the agency ones pass untouched | |
| SEC-11 | Browser content | PASS | The CSS rule sets background, border, radius, padding and shadow from existing tokens on an existing class. No content is injected, no selector reaches outside the table's zones, and no game-supplied string reaches a style | A palette value is a colour from the design canvas, written in the glue, not taken from game data |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned for local paths, addresses and token-like strings: none | |
| SEC-18 | Dependencies and supply chain | PASS | No dependency, script or lockfile change | |

## Findings

None.

## Execution

Commands/tools and versions: `npx tsc --noEmit` for the web app;
`npx vitest run --root apps/web` (vitest 2.1.9); one throwaway Playwright run
opening a table in three games.
Tests passed/failed/skipped; real engine versus stub: 92 passed, 0 failed, 0
skipped. The browser run opened Cybernoir, Fractured Fist and Sweetlands
Imperium against the real engine and screenshotted each: all three read as
panels and none broke. Sweetlands refused the first attempt because the script
picked the same faction for two seats — the setup page working as intended,
not a regression.
Browser/transport/configuration coverage: Chromium through Playwright at
1440×900 over HTTP and websockets, SQLite storage.
Dependency and secret scan scope/date: 2026-09-22, the staged diff only.
Checks blocked/not run and exact reason: none beyond the standing ones —
Postgres remains unexercised.
Prior findings rechecked, fixed, reopened, or still unverified: none; this
change touches none of their surfaces.
Files changed and temporary-resource cleanup: the three files in the staged
diff. Throwaway browser specs and screenshots live in a scratch directory
outside the repository. The dev stack started for these checks was stopped
afterwards.
Next required review: full, at the first development session of the coming
week, and before accepting any milestone.
