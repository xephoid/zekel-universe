# Security review — 2026-09-22

Mode: change (items 3 and 4 of the Cybernoir table's design pass: the case
file as the points track, and cards carrying their own facts)
Commit and working-tree scope: the working tree on top of commit 565c12c on
`main`, committed right after this report. Changed surfaces here: one glue
module (`apps/web/src/glue/cybernoir-2127.ts`), its fixture tests, and three
files of the shared primitives (two optional `CardZoneData` fields, one
optional `CardData` field, and their styles). The paired engine change is
`d7165d6` in the sibling engine repository: `EVIDENCE_SHAPE` / `EVIDENCE_TOTAL`
named once and published in the reference data.
Environment: local machine. Unit and page tests under jsdom; browser checks
against the local stack — the rebuilt engine on 8787, the server, and Vite —
with a guest session, played from both seats.
Prior full review: none; the prior change review is
[2026-09-22-ca3d616-change.md](2026-09-22-ca3d616-change.md).
Overall result: no issues found in the tested scope.
Release recommendation: not applicable (no deployment yet).

## Coverage

One item here touches a privacy surface on purpose — the Detective's own
informants are now drawn by name for their owner — so SEC-07 is the point of
the review. Selected: SEC-01, SEC-07, SEC-09, SEC-11, SEC-14 and SEC-18.
SEC-02, SEC-05, SEC-08, SEC-10, SEC-13, SEC-16 and SEC-17 cover surfaces
nothing here touches: no route, socket event, authorization rule, input,
stored row or log line changes. SEC-03 through SEC-06, SEC-12, SEC-15, SEC-19
through SEC-22 are outside this change's scope.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No route, socket event or handler added. The engine gains one field in an existing reference-data response, which is public game data (the printed shape of the win) | |
| SEC-07 | The Detective's own informants | PASS (reproduced) | The identities were already in the Detective's own view and only theirs; the change is that their client draws them instead of showing a card back to their owner. No view, payload or filter changed, so nothing moved for the Hacker: the two-human privacy spec and the socket privacy tests assert on the wire, not the screen, and are untouched. `glue.test.ts` pins that the names appear only in the Detective's own plan, each with a line saying whether the Hacker has seen it | The spec was not re-run for this change; no wire surface changed. Re-run it with the next pass that does |
| SEC-07 | The Hacker's informant count | PASS | `informants_facedown_count` is in the **public** view and was already sent to both seats; the Hacker's panel now shows the count that is theirs to know. Names are not in their view and are not drawn | |
| SEC-07 | The case file | PASS (reproduced) | Drawn from the `evidence` block of the view, which is public — the Evidence zone is face up on the table by the rules — and the row sizes from public reference data. The Detective sees the same thirteen, asserted in `glue.test.ts` | |
| SEC-07 | Cards' facts | PASS | Cost, affiliation, ability and residents all come from `reference_data`, which is the printed cards and public to both seats. Nothing per-card is derived from hidden state; in particular no per-card block risk is drawn, because no one can know it | |
| SEC-09 | Game actions and agency | PASS | No move, template or lit part changes; the case file's rows are not selectable. The agency tests still pass untouched | |
| SEC-11 | Browser content | PASS | Every string drawn comes from the engine's reference data or its view, through React's own escaping. A person or location the reference data does not carry is drawn as its bare name rather than given facts it does not have | |
| SEC-14 | Secrets and published artifacts | PASS | Changed files scanned for local paths, addresses and token-like strings: none | |
| SEC-18 | Dependencies and supply chain | PASS | No dependency, script or lockfile change | |

## Findings

None.

## Execution

Commands/tools and versions: `npx tsc --noEmit` for the web app, the primitives
package and the engine; `npx vitest run --root apps/web` (vitest 2.1.9);
`npx vitest run src/games/cybernoir-2127` in the engine; a throwaway Playwright
run covering both seats.
Tests passed/failed/skipped; real engine versus stub: 77 passed here, 0 failed,
0 skipped; 260 passed in the engine's Cybernoir suite. The browser checks ran
against the real engine and asserted the thirteen slots, the `0 / 13` count, a
cost pip on a Contact, the Hacker's informant count, and a Location's residents
and facts in the Detective's hand.
Browser/transport/configuration coverage: Chromium through Playwright at
1440×900 over HTTP and websockets, SQLite storage.
Dependency and secret scan scope/date: 2026-09-22, the staged diff only; no
dependency change to scan.
Checks blocked/not run and exact reason: two of item 3's acceptance points —
a completed set landing as three cards at once, and a bounced set emptying its
slots — were not reached, because both need a long game; recorded as unmet in
the build notes rather than assumed. The two-browser privacy spec was not
re-run, as no wire surface changed. Postgres remains unexercised.
Prior findings rechecked, fixed, reopened, or still unverified: none; this
change touches none of their surfaces.
Files changed and temporary-resource cleanup: the six files in the staged diff,
plus the engine commit named above. Throwaway browser specs and screenshots
live in a scratch directory outside the repository. The dev stack started for
these checks was stopped afterwards.
Next required review: full, at the first development session of the coming
week, and before accepting any milestone.
