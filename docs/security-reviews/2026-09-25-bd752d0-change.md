# Security review — 2026-09-25

Mode: change (NGnG table after play: the opening log entry, standings and
the last move in the side column, End turn after a finished action, the
treaty proposer's mark, wrapping buttons)
Commit and working-tree scope: the working tree on `main` over `bd752d0`,
committed right after this report. Changed surfaces: the server's opening
event (`openingSummary` in `realtime.ts`), the table page's side column, the
glue types, the NGnG reader, router, layout, round screens and styles, and
tests. The paired engine change is on the engine branch
`claude/ngg-action-finished` (one public field on the view).
Environment: local machine. Server tests in-process with the fake engine;
page tests under jsdom; screenshots of a live table against the local engine
(built from that branch), the restarted server and Vite.
Prior full review: none. The latest change review is
[2026-09-25-40f68a8-change.md](2026-09-25-40f68a8-change.md). A full review
is still due.
Overall result: no findings.
Release recommendation: not applicable (no deployment yet).

## Coverage

The opening entry now carries engine text to every seat, so SEC-07 and
SEC-10 carry the weight. Selected: SEC-01, SEC-02, SEC-07, SEC-09, SEC-10,
SEC-11, SEC-14, SEC-18.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-01 | Entry points | PASS | No route or socket event added | |
| SEC-02 | Authorization | PASS | Moves still go through `submitMove`; End turn sends the engine's listed finish | |
| SEC-07 | Hidden information | PASS | The opening entry uses the engine's public log (the engine's contract keeps secrets out of it; its log commit acbcacb). The finished flag is public (the log already says the action is complete) | Relies on the engine keeping its log public-only |
| SEC-09 | Agency | PASS | End turn is shown only when the engine says the action is finished and sends only the listed move (`ngg-round.test.tsx`); nothing is decided for the player | |
| SEC-10 | Validation | PASS | `openingSummary` keeps at most 20 entries, each a non-empty string cut to 300 characters; other shapes are dropped (`opening-summary.test.ts`) | |
| SEC-11 | Browser content | PASS | Summaries render as text; no raw HTML | |
| SEC-14 | Secrets and artifacts | PASS | Diff searched for credentials and local paths: none | |
| SEC-18 | Dependencies | PASS | No dependency or lockfile change | |

## Execution

Commands/tools and versions: `npx tsc --noEmit` for web and server; `npx
vitest run` in `apps/web` (324 passed) and `apps/server` (74 passed, 1
skipped) (vitest 2.1.9); the engine's NGnG suite on its branch (259
passed); Playwright (Chromium) screenshots of a live table, light and dark.
Dependency and secret scan scope/date: 2026-09-25, the working-tree diff.
