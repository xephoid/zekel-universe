# Security review — 2026-09-23

Mode: change, documentation (the Table Now artboard recaptured against the
current app, with new pins and notes)
Commit and working-tree scope: the working tree on top of commit 40a66e9 on
`main`, committed right after this report. Two files: the working file
`docs/design/Cybernoir Table Now.dc.html` and the image it shows,
`docs/design/assets/cybernoir-table-now.jpg`. No application, server, wire,
database, dependency, configuration or engine change.
Environment: local machine. The capture came from a throwaway Playwright run
against the local stack, playing the Hacker against the AI Detective.
Prior full review: none; the prior change review is
[2026-09-23-c970040-change.md](2026-09-23-c970040-change.md).
Overall result: no issues found in the tested scope.
Release recommendation: not applicable (no deployment yet).

## Coverage

A documentation change, so the playbook's documentation mode applies:
disclosure, links, and whether any security instruction changed. The one thing
here that deserves real weight is that the change publishes a screenshot of a
seat's own private view into a public repository. Selected: SEC-07, SEC-14,
SEC-15, SEC-18.

| Check ID | Surface/subcheck | Outcome | Evidence/test | Limitation or activation trigger |
| --- | --- | --- | --- | --- |
| SEC-14 | What the image carries | PASS | 66048 bytes, JPEG, no EXIF block; the only long readable string in the file is `ICC_PROFILE`. Scanned the bytes for `zekes`, `localhost`, `Bearer`, `token`, `@gmail`, `127.0.0.1`, `users` and a drive-letter path: none present. The capture is the page only, with no browser chrome, so no address bar, session id, table id or sign-in link appears | A page screenshot cannot show the URL; if a future capture includes browser chrome this has to be rechecked |
| SEC-14 | What the image shows of a game | PASS | It is a throwaway local table played against the AI, not a real person's game, and nothing in it identifies anybody. The Hacker's own hidden facts are visible — eight contacts and the safehouse at Shizuoka Megamall — which is what that seat is allowed to see and what the artboard is for | Any future capture of a table a real person played would need their say-so before it went into a public repository |
| SEC-07 | What the image shows of the other seat | PASS | The Detective panel carries counts only — action points, location deck 11, location hand 1, person deck 19, mid-game guess spent, Overclock available. No Detective card face appears. The hidden side of the game did not leak into the picture, which is the same boundary the seat view draws | |
| SEC-14 | The working file | PASS | Prose, pin coordinates and an `alt` string. No secret, no personal name, no email, no local path, no link to anything private | |
| SEC-15 | Disclosure | PASS | Note 1 records a bug that is already public in this repository's history and describes a label drawn in the wrong panel — no exploitable detail, nothing about authentication, storage or transport | |
| SEC-18 | Links and dependencies | PASS | No new link. The image reference `assets/cybernoir-table-now.jpg` resolves; the Google Fonts stylesheet in the header is unchanged and pre-existing. No dependency, script or lockfile change | |
| — | Security instructions | PASS | No change to CLAUDE.md, the playbook, or any instruction that governs how this project is built | |

## Findings

None.

## Execution

Commands/tools and versions: a Playwright Chromium run to recapture the table
and a second one to check pin placement; a Chromium canvas to downscale the
capture; a byte scan of the committed JPEG. No test suite was run, because no
code changed.
Tests passed/failed/skipped; real engine versus stub: not applicable. The
capture itself came from the real engine and the real server, played through
the browser rather than stubbed.
Browser/transport/configuration coverage: Chromium at 1440×900 for the
capture, downscaled to 1200×750 to match the size the artboard replaced.
Dependency and secret scan scope/date: 2026-09-23, both changed files,
including a byte-level scan of the image rather than a text scan.
Checks blocked/not run and exact reason: the reassemble-and-republish step
named in CLAUDE.md was not run — no `design` skill exists in this session, and
`.claude/` holds only `launch.json`. The `.dc.html` working file was edited
directly, which is the sanctioned way to change the design; only the
republished output is missing, and no assembled output file was hand-edited.
The artboard could not be rendered locally to confirm, because `support.js` is
supplied by the canvas host and is not in the repository; pin placement was
checked instead against a standalone copy of the same image, box size and
absolute offsets.
Prior findings rechecked, fixed, reopened, or still unverified: none reopened.
Seven of the eight problems the previous capture pinned are confirmed gone in
the new one; the eighth, the Detective's phantom Overclock, is still open and
is now note 1.
Files changed and temporary-resource cleanup: the two files named above. The
throwaway capture script, the shrink script and the pin-check page live in the
session scratch directory outside the repository; the temporary module used to
drive Chromium from the repository root was deleted, and `git status` shows
only the two intended files. The dev stack was left running.
Next required review: full, at the first development session of the coming
week, and before accepting any milestone.
